


class Processor:
	
	def __init__(self):
		self.docset = {}
		self.symbols = set()	


	def add_to_docset(fullpath):
		"""
		Add all entities within the file given by `fullpath` to the docset
		"""
		pass


	def generate_json_doc(self, output_folder):
		"""
		Generate json infosets for all entries in the docset and writes them
		to `output_folder`.
		"""
		pass




